import { AboutPageContent } from "@/components/ui/about-page-content";

export const metadata = {
  title: "About — Nzali",
  description: "The hand behind the line.",
};

export default function AboutPage() {
  return <AboutPageContent />;
}
