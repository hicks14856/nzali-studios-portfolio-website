import { GalleryPageContent } from "@/components/ui/gallery-page-content";

export const metadata = {
  title: "Gallery — Nzali",
  description: "Full catalogue of works across three chapters.",
};

export default function GalleryPage() {
  return <GalleryPageContent />;
}
