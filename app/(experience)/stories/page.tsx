import { StoriesPageContent } from "@/components/ui/stories-page-content";

export const metadata = {
  title: "Stories — Nzali",
  description: "Memory chapters — stories kept in the line.",
};

export default function StoriesPage() {
  return <StoriesPageContent />;
}
