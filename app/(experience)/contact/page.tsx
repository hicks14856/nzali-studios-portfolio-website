import { ContactPageContent } from "@/components/ui/contact-page-content";

export const metadata = {
  title: "Contact — Nzali",
  description: "Commissions and private inquiries.",
};

export default function ContactPage() {
  return <ContactPageContent />;
}
